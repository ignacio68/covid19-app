import { countriesList } from '@/repository'
import { Icountry } from './interfaces'

// class CountryClass implements Country {

// }

export class Repository {
         private countriesList = countriesList

        //  private getCountry(countryName: string) {
        //    const returnCountry = this.countriesList.filter(countryListed => countryListed.name === countryName)
        //    const country: Icountry = { returnCountry.code21, returnCountry.name, returnCountry.latitude, returnCountry.longitude }
        //    return country
        //   }
        //  public getCountryCoordenates(country: string): object {
        //    let countryCoordinates = { lat: '', lng: '' }
        //    const myCountry = this.getCountry(country)
        //    console.log(JSON.stringify(myCountry))
        //   //  countryCoordinates.lat = myCountrylatitude
        //   //  countryCoordinates.lng = myCountry.longitude
        //    return countryCoordinates
        //  }
       }
